#include "printf.h"
#include "converters/converters.h"
#include "stdio.h"

void jump()
{
    printf("\n");
}

int main(void)
{
    // int positive = 42;

    jump();

    // printf("number: %i", 
    // printf("|%x|", &positive));

    // char *result = hexadecimal(&positive, 0);

    // printf("|%p\n", &positive);
    // printf("|%ld\n", &positive);
    // printf("|%x\n", &positive);
    // printf("|%s\n", result);

    // jump();

    // printf("number: %i",
    // ft_printf("|%p|", positive));

    // jump();
    // jump();

    return (1);
}
